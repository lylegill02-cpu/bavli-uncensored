# Copy to .env (gitignored). Never commit real values.

# Optional — only if loading corpus into Supabase (not needed for GitHub Pages)
SUPABASE_DB_URL=postgresql://postgres.[ref]:[password]@aws-0-....supabase.co:5432/postgres

# Optional — CLI search via Supabase REST (anon key is enough for read-only RPCs)
SUPABASE_URL=https://galhchvctvmtbykzzqqp.supabase.co
SUPABASE_ANON_KEY=
# Service role bypasses RLS — keep local only, never commit
SUPABASE_SERVICE_ROLE_KEY=
